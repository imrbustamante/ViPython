verificar se o Python já está instalado no computador digitando com prompt de comando o 
\python

Caso esteja será necessário sair do pyhton com o comando exit()

instalar a biblioteca cv que trabalha com imagens em python
python -m pip install opencv-python

Caso o comando não reconheça o pip será necessário instalar para isso baixe o pip  nesse link https://bootstrap.pypa.io/get-pip.py e execute o comando 
Abra o prompt de comandos e navegue até a pasta onde você salvou o get-pip.py
Execute o seguinte comando
python get-pip.py